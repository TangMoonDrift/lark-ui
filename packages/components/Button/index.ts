import Button from './Button.vue'
import ButtonGroup from './ButtonGroup.vue'
import { withInstall } from '@lark-ui/utils'

export const LarkButton = withInstall(Button)
export const LarkButtonGroup = withInstall(ButtonGroup)

export * from './types'
